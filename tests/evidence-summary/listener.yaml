---
apiVersion: tekton.dev/v1beta1
kind: EventListener
metadata:
  name: listener-evidence-summary
spec:
  triggers:
    - binding:
        name: binding-evidence-summary
      template:
        name: template-evidence-summary
